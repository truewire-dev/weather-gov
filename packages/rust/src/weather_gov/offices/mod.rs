//! Generated by truewire — do not edit by hand.

pub mod get_office;

use std::sync::Arc;

use truewire_core::{serde_json, CallOptions, HttpEndpoint, Result};

use crate::meta::DefaultMeta;

/// The weather forecast offices that issue everything else here.
///
/// See <https://www.weather.gov/documentation/services-web-api#/default/office>.
#[derive(Clone)]
pub struct Offices {
    get_office: get_office::GetOffice,
}

impl Offices {
    pub fn new(core: Arc<dyn HttpEndpoint<DefaultMeta>>) -> Self {
        Self {
            get_office: get_office::GetOffice::new(core),
        }
    }

    /// One weather forecast office: who to contact, and everything it is responsible for.
    ///
    /// See <https://www.weather.gov/documentation/services-web-api#/default/office>.
    pub async fn get_office(
        &self,
        request: get_office::Request,
        options: CallOptions,
    ) -> Result<get_office::Office> {
        self.get_office.get_office(request, options).await
    }

    /// `get_office` without validation: the wire body as it came.
    pub async fn get_office_raw(
        &self,
        request: get_office::Request,
        options: CallOptions,
    ) -> Result<serde_json::Value> {
        self.get_office.get_office_raw(request, options).await
    }
}

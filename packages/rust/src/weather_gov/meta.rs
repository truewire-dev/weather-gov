//! Generated by truewire — do not edit by hand.
//!
//! Per-endpoint `meta` shapes, one struct per `[cores.<name>]` in `truewire.toml` that declares a `meta` schema. The hand-written core implements `HttpEndpoint<XMeta>` for its core's struct; generated calls pass a matching value.

/// `meta` for every endpoint whose nearest `router.json` resolves to the `default` core.
#[derive(Debug, Clone, PartialEq)]
pub struct DefaultMeta {
    pub payload: Option<String>,
}

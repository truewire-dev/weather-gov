// Generated by truewire — do not edit by hand.
import type { CallOptions, HttpEndpoint } from '@truewire/core'
import type { DefaultMeta } from '../meta.js'
import type { AlertFeature } from '../types/index.js'
import * as getActiveAlerts from './get_active_alerts.js'
import * as getAlert from './get_alert.js'

/**
 * Watches, warnings and advisories in effect.
 *
 * @see https://www.weather.gov/documentation/services-web-api#/default/alerts_active
 */
export class Alerts {
  private readonly getActiveAlerts_: getActiveAlerts.GetActiveAlerts
  private readonly getAlert_: getAlert.GetAlert

  constructor(readonly core: HttpEndpoint<DefaultMeta>) {
    this.getActiveAlerts_ = new getActiveAlerts.GetActiveAlerts(core)
    this.getAlert_ = new getAlert.GetAlert(core)
  }

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  getActiveAlerts(request: getActiveAlerts.Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Every watch, warning and advisory in effect right now, optionally narrowed. Unnarrowed this is the whole country and runs to a few hundred alerts, so narrow by `area`, `zone` or `point` unless you want all of it.
   *
   * @see https://www.weather.gov/documentation/services-web-api#/default/alerts_active
   */
  getActiveAlerts(request?: getActiveAlerts.Request, options?: CallOptions): Promise<getActiveAlerts.AlertCollection>
  getActiveAlerts(request?: getActiveAlerts.Request, options?: CallOptions): Promise<getActiveAlerts.AlertCollection> {
    return this.getActiveAlerts_.getActiveAlerts(request, options)
  }

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  getAlert(request: getAlert.Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * One alert by its identifier, including the polygon it was drawn for when it has one. The identifiers come from `alerts.get_active_alerts`.
   *
   * @see https://www.weather.gov/documentation/services-web-api#/default/alerts_single
   */
  getAlert(request: getAlert.Request, options?: CallOptions): Promise<AlertFeature>
  getAlert(request: getAlert.Request, options?: CallOptions): Promise<AlertFeature> {
    return this.getAlert_.getAlert(request, options)
  }
}

// Generated by truewire — do not edit by hand.
/** Per-endpoint `meta` shapes, one interface per `[cores.<name>]` in `truewire.toml` that declares a `meta` schema. The hand-written core takes its core's interface as the `Meta` parameter of `HttpEndpoint`; generated calls pass a matching object literal. */

/** `meta` for every endpoint whose nearest `router.json` resolves to the `default` core. */
export interface DefaultMeta {
  payload?: string
}

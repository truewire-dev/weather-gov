// Generated by truewire — do not edit by hand.
import type { CallOptions, HttpEndpoint } from '@truewire/core'
import type { DefaultMeta } from '../meta.js'
import * as getPoint from './get_point.js'

/**
 * Turning a latitude/longitude into the grid the rest of the API is addressed by.
 *
 * @see https://www.weather.gov/documentation/services-web-api#/default/point
 */
export class Points {
  private readonly getPoint_: getPoint.GetPoint

  constructor(readonly core: HttpEndpoint<DefaultMeta>) {
    this.getPoint_ = new getPoint.GetPoint(core)
  }

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  getPoint(request: getPoint.Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Resolve a latitude/longitude to the forecast grid it falls in. Almost every other endpoint here is addressed by grid cell rather than by coordinates, so this is where a caller starts: it returns the office and the grid indices `forecast.get_forecast` takes, plus the zones, the radar station and the nearest city.
   *
   * @see https://www.weather.gov/documentation/services-web-api#/default/point
   */
  getPoint(request: getPoint.Request, options?: CallOptions): Promise<getPoint.Point>
  getPoint(request: getPoint.Request, options?: CallOptions): Promise<getPoint.Point> {
    return this.getPoint_.getPoint(request, options)
  }
}

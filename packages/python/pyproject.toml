[project]
name = "truewire-weather-gov"
version = "0.1.0"
description = "Typed, validated Python client for the US National Weather Service API, generated by Truewire."
# No `readme`: this package's documentation is the repository root README, which is the
# Python page of the language nav, and setuptools refuses a readme outside the package
# directory.
license = "MIT"
requires-python = ">=3.11"
dependencies = ["truewire-core>=0.2.1,<0.3"]
keywords = ["weather", "nws", "noaa", "forecast", "weather.gov", "api-client", "truewire"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Atmospheric Science",
  "Typing :: Typed",
]

[project.urls]
Homepage = "https://github.com/truewire-dev/weather-gov"
Truewire = "https://truewire.dev"

[project.optional-dependencies]
dev = [
  "truewire==0.9.0",
  "pytest>=8",
  "pytest-asyncio>=0.24",
  "pyright>=1.1.390",
  "ruff>=0.8",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
weather_gov = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["test"]
asyncio_mode = "strict"
pythonpath = ["src"]

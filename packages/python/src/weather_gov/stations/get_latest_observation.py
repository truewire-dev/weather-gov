# Generated by truewire — do not edit by hand.
from typing_extensions import Any, Literal, NotRequired, TypedDict, overload
from weather_gov.core import Endpoint
from weather_gov.schemas import Observation


class Request(TypedDict):
  """Which station."""

  station_id: str
  """Station identifier, such as `KSEA`."""
  require_qc: NotRequired[bool]
  """Return only an observation that has passed quality control. Off by default, which is what you want for a current reading: quality control lags the observation."""


class GetLatestObservation(Endpoint):
  """The most recent observation from one station. This is the endpoint behind "what is it doing outside right now"."""

  @overload
  async def get_latest_observation(
    self,
    station_id: str,
    *,
    require_qc: bool | None = None,
    validate: Literal[False],
  ) -> Any: ...
  @overload
  async def get_latest_observation(
    self,
    station_id: str,
    *,
    require_qc: bool | None = None,
    validate: bool | None = None,
  ) -> Observation: ...
  async def get_latest_observation(
    self,
    station_id: str,
    *,
    require_qc: bool | None = None,
    validate: bool | None = None,
  ) -> Observation:
    """The most recent observation from one station. This is the endpoint behind "what is it doing outside right now".

    Args:
      station_id: Station identifier, such as `KSEA`.
      require_qc: Return only an observation that has passed quality control. Off by default, which is what you want for a current reading: quality control lags the observation.
      validate: Override this call's response validation; falls back to the client-level default when omitted. `False` returns the parsed body as it came, typed `Any`.

    References:
      - [Official docs](https://www.weather.gov/documentation/services-web-api#/default/station_observation_latest)
    """
    request: Request = Request(station_id=station_id)
    if require_qc is not None:
      request['require_qc'] = require_qc
    return await self.request(
      request,
      method='GET',
      path='/stations/{station_id}/observations/latest',
      meta={'payload': 'properties'},
      validate=validate,
      request_type=Request,
      response_type=Observation,
    )

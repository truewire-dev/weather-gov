# Generated by truewire — do not edit by hand.
from typing_extensions import Any, Literal, TypedDict, overload
from weather_gov.core import Endpoint
from weather_gov.schemas import AlertFeature


class Request(TypedDict):
  """Which alert."""

  id: str
  """Alert identifier, such as `urn:oid:2.49.0.1.840.0.<hash>.001.1`. `alerts.get_active_alerts` returns it as `properties.id`."""


class GetAlert(Endpoint):
  """One alert by its identifier, including the polygon it was drawn for when it has one. The identifiers come from `alerts.get_active_alerts`."""

  @overload
  async def get_alert(self, id: str, *, validate: Literal[False]) -> Any: ...
  @overload
  async def get_alert(
    self,
    id: str,
    *,
    validate: bool | None = None,
  ) -> AlertFeature: ...
  async def get_alert(self, id: str, *, validate: bool | None = None) -> AlertFeature:
    """One alert by its identifier, including the polygon it was drawn for when it has one. The identifiers come from `alerts.get_active_alerts`.

    Args:
      id: Alert identifier, such as `urn:oid:2.49.0.1.840.0.<hash>.001.1`. `alerts.get_active_alerts` returns it as `properties.id`.
      validate: Override this call's response validation; falls back to the client-level default when omitted. `False` returns the parsed body as it came, typed `Any`.

    References:
      - [Official docs](https://www.weather.gov/documentation/services-web-api#/default/alerts_single)
    """
    return await self.request(
      Request(id=id),
      method='GET',
      path='/alerts/{id}',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=AlertFeature,
    )
